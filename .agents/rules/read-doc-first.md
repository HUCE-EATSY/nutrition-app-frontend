---
trigger: always_on
---

## Mandatory First Step: Read Documentation
Before executing any task, starting any implementation, or modifying any code, the agent MUST explicitly read and review the relevant contents within the `.agents/` directory (including `rules/`, `skills/`, `workflows/`, and `knowledge/`). This ensures proper alignment with the project's design specifications, blueprints, and implementation plans.
