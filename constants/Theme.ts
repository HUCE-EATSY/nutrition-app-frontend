import { colors, spacing, radius, typography, shadows, gradients, layout } from './index';

export const Theme = {
  colors,
  spacing,
  radius,
  typography,
  shadows,
  gradients,
  layout,
};

export default Theme;
